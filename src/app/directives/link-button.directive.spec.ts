import { LinkButtonDirective } from './link-button.directive';

describe('LinkButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkButtonDirective();
    expect(directive).toBeTruthy();
  });
});
